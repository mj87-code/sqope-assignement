fastapi==0.138.1
uvicorn[standard]==0.49.0

# LangChain ecosystem
langchain==1.3.11
langchain-anthropic==1.4.8
langchain-core==1.4.8
langchain-huggingface==1.2.2

# Anthropic SDK
anthropic==0.112.0

# PDF parsing (indexer only — see Dockerfile.indexer; the API never parses
# PDFs and doesn't need docling's much larger dependency tree)

# Database
sqlalchemy==2.0.51
asyncpg==0.31.0
psycopg2-binary==2.9.12
pgvector==0.4.2

# Embeddings (torch installed separately in Dockerfile for CPU-only build)
sentence-transformers==5.6.0

# Data
pandas==3.0.4

# Validation / config
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.20
